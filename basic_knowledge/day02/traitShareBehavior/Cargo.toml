[package]
name = "traitShareBehavior"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
# [lib]
# name = "lib"  # 替换为你的库的名称
# path = "src/lib.rs"  # 替换为你的模块文件的路径

[dependencies]
